---
title: "List Settlements"
openapi: "GET /payouts/settlements"
---

List on-chain settlement records. Filter by payout intent or chain.

<CodeGroup>

```bash cURL
curl "https://api.billing.io/v1/payouts/settlements?payout_intent_id=po_a1b2c3d4e5f6" \
  -H "Authorization: Bearer $BILLING_API_KEY"
```

```javascript Node.js
const response = await fetch(
  "https://api.billing.io/v1/payouts/settlements?payout_intent_id=po_a1b2c3d4e5f6",
  { headers: { "Authorization": `Bearer ${process.env.BILLING_API_KEY}` } }
);
const { data } = await response.json();
```

</CodeGroup>
